<?xml version="1.0"?>
<ruleset name="SendToMP">
	<description>WordPress coding standards for the SendToMP plugin.</description>

	<!-- Scan plugin files -->
	<file>.</file>

	<!-- Exclude non-plugin files -->
	<exclude-pattern>/vendor/*</exclude-pattern>
	<exclude-pattern>/node_modules/*</exclude-pattern>
	<exclude-pattern>/.git/*</exclude-pattern>
	<exclude-pattern>/.github/*</exclude-pattern>

	<!-- Use WordPress coding standards -->
	<rule ref="WordPress">
		<!-- Allow short array syntax -->
		<exclude name="Generic.Arrays.DisallowShortArraySyntax"/>
		<!-- Allow short ternaries -->
		<exclude name="WordPress.PHP.DisallowShortTernary"/>
	</rule>

	<!-- Check for WordPress VIP compatibility (informational) -->
	<rule ref="WordPress-Extra"/>

	<!-- Verify text domain -->
	<rule ref="WordPress.WP.I18n">
		<properties>
			<property name="text_domain" type="array">
				<element value="sendtomp"/>
			</property>
		</properties>
	</rule>

	<!-- Minimum supported PHP version -->
	<config name="minimum_php_version" value="7.4"/>

	<!-- Minimum supported WordPress version -->
	<config name="minimum_wp_version" value="6.0"/>

	<!-- Prefixes for functions, classes, and hooks -->
	<rule ref="WordPress.NamingConventions.PrefixAllGlobals">
		<properties>
			<property name="prefixes" type="array">
				<element value="sendtomp"/>
				<element value="SendToMP"/>
				<element value="SENDTOMP"/>
			</property>
		</properties>
	</rule>

	<!-- Use tabs for indentation -->
	<arg name="tab-width" value="4"/>
	<rule ref="Generic.WhiteSpace.DisallowSpaceIndent"/>

	<!-- Show progress and use colours -->
	<arg value="ps"/>
	<arg name="colors"/>
	<arg name="extensions" value="php"/>
</ruleset>
